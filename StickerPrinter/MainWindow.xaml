<Window x:Class="StickerPrinter.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:models="clr-namespace:StickerPrinter.Models"
        mc:Ignorable="d"
        Title="StickerPrinter"
        Height="720"
        Width="1280"
        MinHeight="720"
        MinWidth="1280"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </Window.Resources>

    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" MinWidth="380" />
            <ColumnDefinition Width="6" />
            <ColumnDefinition Width="420" MinWidth="320" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock Text="ZPL editor"
                       FontSize="18"
                       FontWeight="SemiBold"
                       Margin="0,0,0,10" />

            <TextBox Grid.Row="1"
                     Text="{Binding ZplCode, UpdateSourceTrigger=PropertyChanged}"
                     FontFamily="Consolas"
                     FontSize="14"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     TextWrapping="NoWrap"
                     AcceptsReturn="True"
                     AcceptsTab="True" />
        </Grid>

        <GridSplitter Grid.Column="1"
                      Width="6"
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Stretch"
                      Background="#FFE0E0E0" />

        <Grid Grid.Column="2" Margin="18,0,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0">
                <TextBlock Text="Settings"
                           FontSize="18"
                           FontWeight="SemiBold"
                           Margin="0,0,0,12" />

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="90" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0"
                               Grid.Column="0"
                               Text="Width:"
                               VerticalAlignment="Center"
                               Margin="0,0,12,8" />
                    <TextBox Grid.Row="0"
                             Grid.Column="1"
                             Text="{Binding Width, UpdateSourceTrigger=PropertyChanged}"
                             Margin="0,0,0,8" />

                    <TextBlock Grid.Row="1"
                               Grid.Column="0"
                               Text="Height:"
                               VerticalAlignment="Center"
                               Margin="0,0,12,8" />
                    <TextBox Grid.Row="1"
                             Grid.Column="1"
                             Text="{Binding Height, UpdateSourceTrigger=PropertyChanged}"
                             Margin="0,0,0,8" />

                    <TextBlock Grid.Row="2"
                               Grid.Column="0"
                               Text="Density:"
                               VerticalAlignment="Center"
                               Margin="0,0,12,0" />
                    <ComboBox Grid.Row="2"
                              Grid.Column="1"
                              ItemsSource="{Binding DensityOptions}"
                              SelectedItem="{Binding SelectedDensity}"
                              MinHeight="28">
                        <ComboBox.ItemTemplate>
                            <DataTemplate DataType="{x:Type models:RenderDensity}">
                                <TextBlock>
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="8 dpmm" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding}" Value="{x:Static models:RenderDensity.Dpmm12}">
                                                    <Setter Property="Text" Value="12 dpmm" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding}" Value="{x:Static models:RenderDensity.Dpmm24}">
                                                    <Setter Property="Text" Value="24 dpmm" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                </Grid>
            </StackPanel>

            <StackPanel Grid.Row="1"
                        Orientation="Horizontal"
                        Margin="0,22,0,18">
                <Button Content="Создать"
                        Command="{Binding RenderCommand}"
                        MinWidth="120"
                        Height="34"
                        Margin="0,0,10,0" />
                <Button Content="Download PNG"
                        Command="{Binding DownloadPngCommand}"
                        MinWidth="130"
                        Height="34" />
            </StackPanel>

            <Border Grid.Row="2"
                    BorderBrush="#FFCFCFCF"
                    BorderThickness="1"
                    Background="#FFF7F7F7"
                    Padding="14">
                <Grid>
                    <TextBlock Text="{Binding PreviewMessage}"
                               TextWrapping="Wrap"
                               TextAlignment="Center"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               Foreground="#FF555555"
                               Visibility="{Binding IsPreviewMessageVisible, Converter={StaticResource BooleanToVisibilityConverter}}" />

                    <Image Source="{Binding PreviewImage}"
                           Stretch="Uniform"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Visibility="{Binding IsPreviewImageVisible, Converter={StaticResource BooleanToVisibilityConverter}}" />
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
